# Memories — output

Store review artifacts here (JSON summaries, waypoints-style exports) so they stay separate from `memories/input/`.

Suggested naming: `review_<stem>.json` or mirror `waypoints/review_findings_<stem>.json` from your workspace.
